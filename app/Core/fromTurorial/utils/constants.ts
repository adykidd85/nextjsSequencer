import { env } from '../utils/env';

//export const BACKEND_URL = env.USE_MOCKS ? '' : env.BACKEND_URL;
export const BACKEND_URL = 'http://stage.sqn.cr/apiFrontEnd';
export const DATE_FORMAT = {
	SCHEDULE_CAMPAIGN: 'yyyy-MM-dd HH:mm:ss',
};
